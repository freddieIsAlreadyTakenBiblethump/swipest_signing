name: swipest_signing
description: Registration module for Swipest.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  email_validator: ^2.0.1
  syncfusion_flutter_datepicker: ^20.1.56
  age_calculator: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true